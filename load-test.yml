config:
  target: 'http://localhost:3000'
  phases:
    - duration: 10  # Duration of the test in seconds
      arrivalRate: 10  # Number of new users per second

scenarios:
  - flow:
      - get:
          url: '/'  # Request the root URL
